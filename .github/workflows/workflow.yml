name: my node.js app workflow

on: push

jobs:
    docker:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout code
          uses: actions/checkout@v3
        - name: docker build 
          run: docker build -t docker.io/asomaaroufiniyaa/node-app:latest .

        - name: docker login
          run: docker login --username=asomaaroufiniyaa   --password=aso20017701

        - name: docker publish
          run: docker push docker.io/asomaaroufiniyaa/node-app:latest
    deploy:
        needs: docker
        runs-on: ubuntu-latest
        steps:
        - name: docker Run
          run: |
            docker pull docker.io/asomaaroufiniyaa/node-app:latest
            docker compose -p 1025:1025 up -d
